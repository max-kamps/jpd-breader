document.querySelector('#settings-link').addEventListener('click', () => {
    setTimeout(() => window.close(), 10);
});

document.querySelector('#parse-page').addEventListener('click', (event) => {
    // TODO
});
